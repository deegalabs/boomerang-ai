# Boomerang AI — runtime dependencies (exact pins for reproducible builds).
# Full transitive snapshot lives in requirements.lock; dev/test tools in requirements-dev.txt.

# On-chain core (Filter 2 / BSC validation)
web3==7.16.0

# On-chain agent identity (ERC-8004) via BNB AI Agent SDK
bnbagent==0.3.6

# Brain / decision (Filter 1)
anthropic==0.107.0
mcp==1.27.2            # Model Context Protocol client (CMC Agent Hub)
httpx==0.28.1          # HTTP calls / x402

# Interface (Process A)
python-telegram-bot==21.11.1

# Web dashboard (read-only) + site (landing/docs/console)
starlette==1.2.1
uvicorn==0.49.0
jinja2==3.1.6          # server-side templating (no build step)

# Infra
python-dotenv==1.2.2
